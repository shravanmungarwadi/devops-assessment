# Secrets / local env
.env

# Terraform local state and working files (state lives in the S3 backend,
# never in git)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan.binary
tfplan.txt
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local backup dumps (generated by scripts/backup.sh)
backups/

# OS / editor noise
.DS_Store
*.swp
